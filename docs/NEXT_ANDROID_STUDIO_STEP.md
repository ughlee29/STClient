# Next Android Studio Step

Open branch `feature/v0.1-hearth-spine`.

Do not work on `main`.

First test:

```bash
./gradlew assembleDebug
```

If it fails, fix only build/launch issues first. Do not add features.
