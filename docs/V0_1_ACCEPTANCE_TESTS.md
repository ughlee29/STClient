# LoreHearth v0.1 Acceptance Tests

1. App builds a debug APK.
2. App launches as LoreHearth.
3. Settings screen saves base URL, API key, model ID, temperature, and max tokens.
4. Settings remain after restart.
5. Android file picker imports a JSON character card.
6. Android file picker imports a PNG card with `chara` metadata.
7. Active character is used in the chat prompt.
8. Sending a message makes a real cloud request.
9. Assistant reply displays in chat.
10. Chat remains after restart.
11. Offline Campfire Mode is clearly labeled as future work and does not pretend to run a model.
